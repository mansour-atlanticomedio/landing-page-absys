services:
  frontend:
    build:
      context: .
    ports:
      - "8085:3000"
    volumes:
      - .:/app
      - /app/node_modules
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
    - WATCHPACK_POLLING=true
    - PAYLOAD_SECRET=9fc076e667cb808e40733d2b
    - DATABASE_URL=postgres://postgres:1234@host.docker.internal:5432/payloadcms
    - NEXT_PUBLIC_STRAPI_URL=http://localhost:1337