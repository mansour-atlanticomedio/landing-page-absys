services:
  frontend:
    build: .
    ports:
      - "8085:3000"
    volumes:
      - .:/app
      - /node_modules
    environment:
    - WATCHPACK_POLLING=true
    - PAYLOAD_SECRET=9fc076e667cb808e40733d2b
    - DATABASE_URL=postgres://postgres:1234@127.0.0.1:5432/payloadcms
    - NEXT_PUBLIC_STRAPI_URL=http://localhost:1337