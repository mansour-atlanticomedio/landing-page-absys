services:
  frontend:
    build: ./Dockerfile
    ports:
      - "8081:80"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
    - WATCHPACK_POLLING=true
    restart: always